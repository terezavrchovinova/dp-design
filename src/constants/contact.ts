/**
 * Fallback email used only if the `contact.email` translation key is missing.
 * Keep this in sync with the real address in src/locales/{cs,en}/translation.json.
 */
export const FALLBACK_EMAIL = 'daniela@plaminkova.com'
